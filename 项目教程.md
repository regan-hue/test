# Cornerstone.js Crosshairs 功能实现教程

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [环境准备](#环境准备)
5. [安装步骤](#安装步骤)
6. [配置说明](#配置说明)
7. [代码详解](#代码详解)
8. [使用指南](#使用指南)
9. [常见问题](#常见问题)
10. [参考资源](#参考资源)

---

## 项目概述

本项目使用 **Cornerstone.js** 实现了医学影像的 **Crosshairs（交叉线）** 功能，可以从 **Orthanc DICOM 服务器**加载医学影像数据，并在多个视口中显示多平面重建（MPR）视图。

### 主要功能

- ✅ 从 Orthanc 服务器加载 DICOM 数据
- ✅ 多平面重建（MPR）显示：轴向、矢状、冠状三个视图
- ✅ Crosshairs 工具：多视图同步导航
- ✅ 交互式操作：平移、缩放、窗宽窗位调整、滚动

### 参考示例

本项目参考了 Cornerstone.js 官方示例：[Crosshairs 示例](https://www.cornerstonejs.org/live-examples/crosshairs)

---

## 技术栈

### 核心库

- **Vue 3** (`^3.5.24`) - 前端框架
- **Vite** (`^5.0.0`) - 构建工具和开发服务器

### Cornerstone.js 生态

- **@cornerstonejs/core** (`^1.86.1`) - 核心渲染引擎
- **@cornerstonejs/dicom-image-loader** (`^1.0.0`) - DICOM 图像加载器
- **@cornerstonejs/streaming-image-volume-loader** (`^1.86.1`) - 流式体积加载器
- **@cornerstonejs/tools** (`^1.86.1`) - 工具库（包含 CrosshairsTool）

### 其他依赖

- **dicom-parser** (`^1.8.21`) - DICOM 文件解析器

---

## 项目结构

```
cornoerstone/
├── src/
│   ├── main.js                    # Vue应用入口
│   ├── App.vue                    # 主应用组件
│   └── components/
│       └── CrosshairsViewer.vue   # Crosshairs查看器组件（核心）
├── vite.config.js                 # Vite配置文件（包含代理设置）
├── package.json                   # 项目依赖配置
├── index.html                     # HTML入口文件
└── 项目教程.md                    # 本教程文档
```

---

## 环境准备

### 系统要求

- Node.js >= 16.0.0
- npm >= 7.0.0
- 现代浏览器（支持 SharedArrayBuffer）

### Orthanc 服务器要求

- Orthanc 服务器已安装并运行
- 已启用 DICOMweb 插件（可选，用于 DICOMweb API）
- 服务器地址：`http://192.168.1.3:18997`
- 确保服务器允许远程访问（`RemoteAccessAllowed: true`）

---

## 安装步骤

### 1. 克隆或下载项目

```bash
cd cornoerstone
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置 Orthanc 服务器信息

编辑 `src/components/CrosshairsViewer.vue`，修改以下配置：

```javascript
// DICOM 配置
const orthancUrl = 'http://192.168.1.3:18997'  // 修改为您的 Orthanc 服务器地址
const studyInstanceUID = '您的研究实例UID'
const seriesInstanceUID = '您的系列实例UID'
```

### 4. 启动开发服务器

```bash
npm run dev
```

浏览器会自动打开 `http://localhost:3002`

---

## 配置说明

### Vite 配置 (`vite.config.js`)

#### 1. SharedArrayBuffer 支持

```javascript
headers: {
  'Cross-Origin-Opener-Policy': 'same-origin',
  'Cross-Origin-Embedder-Policy': 'require-corp',
}
```

这些 HTTP 头是必需的，用于启用 SharedArrayBuffer，Cornerstone.js 需要它来高效处理大型医学影像数据。

#### 2. 代理配置

配置了以下代理路径，用于访问 Orthanc API：

- **`/dicom-web`** - DICOMweb API（QIDO-RS, WADO-RS, STOW-RS）
- **`/wado`** - WADO-URI API
- **`/instances`** - 实例文件访问
- **`/series`** - 系列信息访问
- **`/tools`** - 工具接口（find 等）

所有代理都指向 `http://192.168.1.3:18997`，您可以根据实际情况修改。

---

## 代码详解

### 1. 主应用组件 (`App.vue`)

```vue
<template>
  <div id="app">
    <CrosshairsViewer />
  </div>
</template>
```

简单的容器组件，用于挂载 CrosshairsViewer。

### 2. CrosshairsViewer 组件 (`src/components/CrosshairsViewer.vue`)

#### 2.1 模板部分

```vue
<template>
  <div class="crosshairs-viewer">
    <div class="viewport-container">
      <div ref="axialViewport" class="viewport">
        <div class="viewport-label">轴向 (Axial)</div>
      </div>
      <div ref="sagittalViewport" class="viewport">
        <div class="viewport-label">矢状 (Sagittal)</div>
      </div>
      <div ref="coronalViewport" class="viewport">
        <div class="viewport-label">冠状 (Coronal)</div>
      </div>
    </div>
    <div v-if="loading" class="loading">加载中...</div>
    <div v-if="error" class="error">{{ error }}</div>
  </div>
</template>
```

- 三个 viewport 容器：轴向、矢状、冠状
- 加载状态和错误提示

#### 2.2 初始化流程

**步骤 1：初始化 Cornerstone.js**

```javascript
await cornerstoneInit()
await cornerstoneToolsInit()
```

**步骤 2：配置 DICOM 图像加载器**

```javascript
// 设置外部依赖
cornerstoneDICOMImageLoader.external.cornerstone = cornerstone
cornerstoneDICOMImageLoader.external.dicomParser = dicomParser

// 初始化 Web Worker 管理器
cornerstoneDICOMImageLoader.webWorkerManager.initialize({
  maxWebWorkers: navigator.hardwareConcurrency || 4,
  startWebWorkersOnDemand: true,
  taskConfiguration: {
    decodeTask: {
      initializeCodecsOnStartup: false,
      usePDFJS: false,
      strict: false,
    },
  },
})

// 注册图像加载器
cornerstoneDICOMImageLoader.register(imageLoader)
```

**步骤 3：注册体积加载器**

```javascript
volumeLoader.registerVolumeLoader('cornerstoneStreamingImageVolume', cornerstoneStreamingImageVolumeLoader)
```

**步骤 4：创建渲染引擎和工具组**

```javascript
const renderingEngine = new RenderingEngine(renderingEngineId)
const toolGroup = ToolGroupManager.createToolGroup(toolGroupId)
```

**步骤 5：添加和配置工具**

```javascript
// 添加 Crosshairs 工具
addTool(CrosshairsTool)
toolGroup.addTool(CrosshairsTool.toolName)
toolGroup.setToolActive(CrosshairsTool.toolName, {
  bindings: [{ mouseButton: ToolsEnums.MouseBindings.Primary }],
})

// 添加其他工具（平移、窗宽窗位、缩放、滚动）
addTool(PanTool)
addTool(WindowLevelTool)
addTool(ZoomTool)
addTool(StackScrollMouseWheelTool)
// ... 配置工具绑定
```

**步骤 6：创建 Viewports**

```javascript
const viewportInputArray = [
  {
    viewportId: 'axial-viewport',
    type: Enums.ViewportType.ORTHOGRAPHIC,
    element: axialViewport.value,
    defaultOptions: {
      orientation: Enums.OrientationAxis.AXIAL,
    },
  },
  // ... 其他视图
]
renderingEngine.setViewports(viewportInputArray)
```

**步骤 7：加载 DICOM 数据**

```javascript
const imageIds = await fetchInstances()
await loadVolume(renderingEngine, viewportIds, imageIds)
```

#### 2.3 获取 DICOM 实例 (`fetchInstances`)

```javascript
async function fetchInstances() {
  // 1. 使用 Orthanc API 查询系列
  const findResponse = await fetch('/tools/find', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      Level: 'Series',
      Query: { SeriesInstanceUID: seriesInstanceUID },
    }),
  })

  // 2. 获取系列详细信息
  const seriesIds = await findResponse.json()
  const seriesResponse = await fetch(`/series/${seriesIds[0]}`)
  const seriesData = await seriesResponse.json()

  // 3. 构建图像ID列表
  const instanceIds = seriesData.Instances || []
  return instanceIds.map((instanceId) => `wadouri:/instances/${instanceId}/file`)
}
```

**关键点：**
- 使用 Orthanc 的 `/tools/find` API 查询系列
- 通过系列ID获取实例列表
- 构建 `wadouri` 协议的图像ID（WADO-URI）

#### 2.4 加载体积数据 (`loadVolume`)

```javascript
async function loadVolume(renderingEngine, viewportIds, imageIds) {
  const volumeId = `cornerstoneStreamingImageVolume:volume-${seriesInstanceUID}`
  
  // 创建并加载体积
  const volume = await volumeLoader.createAndCacheVolume(volumeId, { imageIds })
  await volume.load()

  // 设置体积到各个viewport
  const viewports = [
    renderingEngine.getViewport(viewportIds.axial),
    renderingEngine.getViewport(viewportIds.sagittal),
    renderingEngine.getViewport(viewportIds.coronal),
  ]
  
  viewports.forEach(viewport => viewport.setVolumes([{ volumeId }]))
  renderingEngine.renderViewports([viewportIds.axial, viewportIds.sagittal, viewportIds.coronal])
}
```

**关键点：**
- 使用 `cornerstoneStreamingImageVolume` 协议创建体积
- 体积加载是异步的，需要等待 `volume.load()` 完成
- 同一个体积可以设置到多个 viewport，实现多平面重建

---

## 使用指南

### 交互操作

| 操作 | 鼠标操作 | 功能 |
|------|---------|------|
| **Crosshairs 导航** | 左键拖动 | 在多视图间同步导航位置 |
| **平移** | 中键拖动 | 平移图像 |
| **窗宽窗位** | 右键拖动 | 调整图像的窗宽窗位 |
| **缩放** | Shift + 左键拖动 | 缩放图像 |
| **滚动** | 鼠标滚轮 | 在图像序列中滚动 |

### Crosshairs 功能说明

- **多视图同步**：在一个视口中点击或拖动 Crosshairs 时，其他视口会自动更新显示对应的交叉线位置
- **实时导航**：可以通过拖动 Crosshairs 来导航到不同的解剖位置
- **视觉反馈**：Crosshairs 以绿色线条显示，清晰标识当前位置

---

## 常见问题

### Q1: 图像无法加载

**可能原因：**
- Orthanc 服务器未运行或无法访问
- 网络连接问题
- 系列实例UID不正确

**解决方法：**
1. 检查 Orthanc 服务器是否运行：访问 `http://192.168.1.3:18997`
2. 检查网络连接和防火墙设置
3. 验证系列实例UID是否正确
4. 查看浏览器控制台的错误信息

### Q2: CORS 错误

**解决方法：**
- 确保 `vite.config.js` 中的代理配置正确
- 检查 Orthanc 服务器的 `RemoteAccessAllowed` 配置
- 使用代理路径访问 API（如 `/series/...` 而不是 `http://192.168.1.3:18997/series/...`）

### Q3: Crosshairs 不显示

**解决方法：**
1. 确保所有三个 viewport 都已添加到工具组
2. 确保 Crosshairs 工具已激活
3. 检查 viewport 是否正确渲染
4. 等待图像加载完成

### Q4: SharedArrayBuffer 错误

**解决方法：**
- 确保 `vite.config.js` 中配置了正确的 HTTP 头
- 确保使用 `http://localhost` 而不是 `http://127.0.0.1` 访问
- 检查浏览器是否支持 SharedArrayBuffer

### Q5: 图像显示为黑色或空白

**可能原因：**
- 窗宽窗位设置不当
- 图像数据未正确加载

**解决方法：**
1. 使用右键拖动调整窗宽窗位
2. 检查浏览器控制台是否有加载错误
3. 验证图像ID格式是否正确

---

## 参考资源

### 官方文档

- [Cornerstone.js 官方文档](https://www.cornerstonejs.org/)
- [Cornerstone.js Crosshairs 示例](https://www.cornerstonejs.org/live-examples/crosshairs)
- [Orthanc DICOMweb 插件文档](https://orthanc.uclouvain.be/book/plugins/dicomweb.html)

### API 参考

- [@cornerstonejs/core API](https://www.cornerstonejs.org/docs/api/core)
- [@cornerstonejs/tools API](https://www.cornerstonejs.org/docs/api/tools)
- [Orthanc REST API](https://orthanc.uclouvain.be/book/users/rest.html)

### 相关库

- [Vue 3 文档](https://vuejs.org/)
- [Vite 文档](https://vitejs.dev/)

---

## 技术要点总结

### 1. Cornerstone.js 初始化顺序

```javascript
1. cornerstoneInit()           // 初始化核心库
2. cornerstoneToolsInit()      // 初始化工具库
3. 配置 DICOM 图像加载器
4. 注册体积加载器
5. 创建渲染引擎和工具组
6. 创建 viewports
7. 加载数据
```

### 2. 图像ID格式

- **WADO-URI**: `wadouri:/instances/{instanceId}/file`
- **DICOMweb**: `dicomweb:/dicom-web/studies/{studyUID}/series/{seriesUID}/instances/{instanceUID}`

### 3. 体积加载协议

- **流式体积**: `cornerstoneStreamingImageVolume:volume-{id}`

### 4. 工具绑定

- **Primary**: 鼠标左键
- **Secondary**: 鼠标右键
- **Auxiliary**: 鼠标中键
- **KeyboardBindings**: Shift, Ctrl, Alt 等修饰键

---

## 项目总结

本项目成功实现了：

1. ✅ **从 Orthanc 服务器加载 DICOM 数据**
   - 使用 Orthanc REST API 查询系列和实例
   - 通过 Vite 代理解决 CORS 问题

2. ✅ **多平面重建（MPR）显示**
   - 轴向、矢状、冠状三个视图
   - 使用 VolumeViewport 实现真正的多平面重建

3. ✅ **Crosshairs 功能**
   - 多视图同步导航
   - 交互式 Crosshairs 工具

4. ✅ **完整的交互功能**
   - 平移、缩放、窗宽窗位调整、滚动

所有功能都基于 Cornerstone.js 官方库实现，代码简洁、可维护性强。

---

## 许可证

本项目使用 MIT 许可证。

---

**最后更新**: 2024年

**作者**: 基于 Cornerstone.js 官方库实现

